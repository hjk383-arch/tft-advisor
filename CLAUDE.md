# LOL Chess — TFT 실시간 추천 프로그램

## 하네스: TFT Advisor

**목표:** 게임 화면을 실시간으로 인식하고, 통계 사이트 데이터 + TypeSafe Jev 판단으로 챔피언·증강·아이템 최선의 선택을 추천하는 Python 앱(`tft_advisor`) 개발·유지보수.

**트리거:** TFT 어드바이저 구현·수정·패치 업데이트·추천/인식 품질 개선 요청 시 `tft-advisor-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**고정 제약:**
- 화면 픽셀만 사용. 게임 메모리 읽기·프로세스 후킹·입력 자동화 금지 (Vanguard).
- TypeSafe API 키는 환경변수 `TYPESAFE_API_KEY`를 먼저 읽고, 없으면 OS 키체인(macOS Keychain / Windows Credential Manager, 폴백은 `~/.config/tft-advisor/`)에서 읽는다. **저장소 안(설정 파일·코드·로그)에는 절대 두지 않는다.** 저장소는 공개다.
- 실시간 모드는 사용자 개인용이며 Riot 서드파티 정책(게임 상태 기반 실시간 추천 금지)에 어긋남을 사용자가 인지하고 선택했다.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-09-21 | 초기 구성 (에이전트 5, 스킬 5) | 전체 | - |
| 2026-09-21 | 최종 덱=보유 아이템·증강 기반 선정(승률은 타이브레이커), 라운드별 빌드업, 오버레이에 목표 덱 1~3개 상시 표시 | skills/jev-recommender, skills/tft-stats-collect, agents/app-integrator | 사용자 도메인 요구사항 |
| 2026-09-23 | API 키: 환경변수 전용 → 환경변수 우선, 없으면 OS 키체인. 설정 화면에 키 입력란 추가 | 고정 제약, app/setup | 개발자가 아닌 사용자(친구)도 자기 키로 테스트할 수 있어야 함 |
