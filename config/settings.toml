# TFT Advisor 실행 설정. 키 정의: src/tft_advisor/config.py (Settings)
# 비밀값(API 키)은 여기 두지 않는다(이 저장소는 공개다). TypeSafe 키는 환경변수 TYPESAFE_API_KEY 우선,
# 없으면 OS 키체인(설정 화면의 [TypeSafe API 키] 칸에서 저장한다) → 폴백 ~/.config/tft-advisor/credentials.toml.
#
# 화면 관련 값은 손으로 고칠 필요가 없다 — 설정 화면이 자동으로 감지해서 **config/settings.local.toml**(이 PC 전용,
# gitignore)에 쓰고, 앱은 그 파일을 이 파일 위에 덮어 읽는다. 이 파일은 공개 저장소의 공용 기본값이다:
#     python -m tft_advisor --setup        (첫 실행에는 자동으로 열리고, 오버레이 트레이 메뉴 "설정"으로도 연다)
# 설정 화면이 쓰는 키: [capture] monitor · [vision] resolution/aspect/profile/content_box/content_box_auto
#                     · [advisor] jev_backend · [overlay] opacity/scale
# 트레이 토글·"게임 화면 다시 찾기"·게임 창 따라가기도 같은 settings.local.toml 에 저장한다(직전 내용은 .bak).
# 이 PC 값을 공용 기본값으로 되돌리려면 settings.local.toml 을 지우면 된다.

[app]
set_number = 18
data_dir = "data"
log_dir = "logs"
state_dir = "_state"     # 세션 상태(_state/session.json)·오버레이 위치(overlay.json). git에 올리지 않는다
# 새 판 판정: 게임 종료/로딩 화면을 한 번 오판해도 세션(수동 입력 증강 포함)을 잃지 않게 한다
reset_strong_confidence = 0.95   # 화면 판별 신뢰도가 이 이상("최종 순위"+"나가기")이면 즉시 새 판
reset_confirm_frames = 3         # 아니면 게임 종료/로딩이 이 횟수 연속 보이거나
reset_confirm_s = 3.0            # 처음 본 뒤 이 초가 지나 다시 보일 때 새 판
reset_recheck_s = 1.0            # 확인 대기 중에는 화면 변화가 없어도 이 주기(초)로 다시 판별
session_archive_keep = 10        # 새 판 때 이전 세션을 _state/sessions/ 에 보관할 개수(0 = 보관 안 함)
# 보유 유닛 장부: 화면의 3D 모델로는 챔피언을 알 수 없어 상점 구매·판매를 추적해서 보유 유닛을 센다
ledger_enabled = true            # false면 board/bench를 채우지 않는다(옛 동작: "보드 미인식")
ledger_settle_s = 2.0            # 상점 변화와 골드 변화가 한 프레임 어긋나도 기다려 주는 시간(초)
ledger_confidence = 0.85         # 설명되지 않은 거래가 없을 때 보유 유닛 신뢰도
ledger_confidence_uncertain = 0.5  # 설명되지 않은 거래가 쌓였을 때 신뢰도(0.6 미만 = advisor가 '모름'으로 처리)
ledger_uncertain_after = 3       # 설명되지 않은 거래가 이만큼 쌓이면 위 값까지 내려간다
ledger_track_sales = true        # 골드가 늘어난 양으로 유닛 판매를 추론할지

[capture]
# 캡처할 모니터. "auto"(기본) = TFT 화면이 있는 모니터를 자동으로 찾는다(스테이지 글자 OCR, 30초마다 재확인).
# 숫자로 못박을 수도 있다: 1 = 주 모니터, 2 = 두 번째 모니터 …, 0 = 모든 모니터를 합친 화면.
# 설정 화면(--setup)의 "모니터" 목록에서 고른 값이 여기 들어간다.
monitor = "auto"
# 창 모드로 띄운 게임 창을 따라갑니다: 실행 중 몇 초마다 게임 창 위치·크기를 확인하고, 옮기거나 크기를 바꾸면
# 모니터·게임 화면 영역(content_box)·해상도를 자동으로 다시 맞춰 settings.local.toml 에 저장합니다(Windows 전용).
# 오버레이 트레이 메뉴 / 인식 확인 창의 "게임 화면 다시 찾기" 버튼, CLI --redetect 로도 한 번 맞출 수 있습니다.
follow_game_window = true
follow_interval_s = 3.0
# 캡처 주기·안정 프레임은 [vision] capture_fps / change_stable_frames (옛 poll_interval_ms·stable_frames 대체)

[vision]
# --- 화면 크기 / 비율 (설정 화면 --setup 이 자동으로 채운다) ----------------
# 게임 해상도. "auto"(기본) = 캡처한 프레임 크기를 그대로 쓴다.
# "1920x1080" 처럼 적으면 그 비율로 ROI를 고르고, 실제 캡처 크기가 다르면 경고한다(캡처 영역 설정 실수 확인용).
resolution = "auto"
# ROI 배치를 고를 화면 비율. "auto"(기본) = 프레임(content_box 적용 후) 비율에서 자동 판별.
# "16:9" | "16:10" | "4:3" | "21:9" | "32:9". 16:9와 16:10만 실제 캡처로 측정했고 나머지는 16:9에서 유도한다(미검증).
# 자동 판별이 틀리면(예: 캡처에 게임 밖 영역이 섞임) 여기서 못박는다.
# 설정 화면은 실측 비율(16:9 · 16:10)이면 "auto"로 두고, 유도 비율일 때만 이름을 적는다.
aspect = "auto"
# 고급: ROI 프로파일 이름을 직접 못박는다. "auto"(기본)가 아니면 aspect·resolution보다 우선한다.
# "set18_16x9" | "set18_16x10" | 해상도 별칭("1920x1080", "1280x800", …)
profile = "auto"
# 게임 화면 영역 [x1, y1, x2, y2] — 캡처 프레임에 대한 비율(0 <= x1 < x2 <= 1, y 동일).
# 생략 또는 [] = 프레임 전체(= [0, 0, 1, 1]). 전체 화면/테두리 없는 창모드면 그대로 둔다.
# 창모드로 바탕화면이 함께 찍히면 게임 영역을 지정한다.
# 예: 2560x1440 모니터의 (200,150) 위치 1600x900 창 → [0.078125, 0.104167, 0.703125, 0.729167]
# 설정 화면에서 "게임 화면 영역을 직접 지정한다"를 켜면 여기에 쓴다(끄면 이 줄을 주석으로 되돌린다).
# content_box = [0, 0, 1, 1]
# content_box가 없을 때 레터박스(위아래/좌우 검은 띠)를 자동으로 잘라낸다. 설정 화면의 같은 이름 체크박스.
content_box_auto = true

shop_fuzzy_min = 85      # rapidfuzz 점수(0~100) 미만이면 None
icon_match_min = 0.8     # 템플릿 매칭 점수 미만이면 None
state_min_confidence = 0.6   # 이보다 낮은 필드는 Jev state에서 제외
ocr_backend = "auto"          # "auto" | "onnxruntime" | "openvino" | "none"(OCR 끔, 진단용)
name_fuzzy_min_margin = 10    # 이름 점수 >= shop_fuzzy_min 이어도 2위와 이만큼 차이 나야 수락 (<= relaxed)
name_fuzzy_relaxed_margin = 15   # 점수 60~85 구간 수락에 필요한 2위와의 차
item_match_margin = 0.05      # 아이템 아이콘 1위와 다른 아이템 1위의 점수 차 하한
change_threshold = 24         # ROI 서명(32x16 회색조) 픽셀 절대차 최댓값이 이보다 크면 변화 (0~255)
change_stable_frames = 2      # 변화 후 이 프레임 수 연속 같아야 재인식 (애니메이션 중 읽지 않음)
capture_fps = 4               # 앱 루프 캡처 주기(Phase 4). 인식·추천은 변화가 있을 때만
traits_every_s = 3            # 앱 루프: 특성 패널을 몇 초에 한 번 읽을지(비싼 ROI)
# 보드·벤치 유닛 챔피언 이름: 특성 패널 인원으로 보드 챔피언 집합을 풀고, 모델 크롭 라이브러리(few-shot)로 칸을 정한다.
unit_names = true
# 증거(특성 패널로 강제된 칸·상점 구매 직후 새 벤치 칸 등)가 있는 유닛 크롭을 data/templates/{set}/units_screen/_pending/
# (검토 대기)에 모은다. 승인 폴더에는 직접 쓰지 않는다 — "유닛 사진 검토" 창(트레이 메뉴·인식 확인 창 버튼,
# 또는 python -m tft_advisor review-units)에서 승인한 사진만 이름 인식에 쓰인다(커밋 안 함)
unit_autolearn = true
# 검토 대기 사진을 이름 순위에 섞는 가중치. 0(기본) = 쓰지 않는다, 0~1 = 닮음 점수에 곱해 순위에만 쓴다(이름을 확정하지는 않음)
unit_pending_weight = 0.0
# true = 상점 구매로 이름이 확실한 벤치 크롭을 검토 없이 바로 승인한다(기본 false: 검토 대기로)
unit_purchase_autoapprove = false
# 사진은 **벤치(대기석)** 칸에서만 모은다. 챔피언·성급별 승인 사진이 이만큼 있으면 그 챔피언은 더 찍지 않는다
# (이미 인식되는 유닛도 찍지 않는다). 새 성급(★2·★3)은 처음 보이면 찍는다. 0 = 제한 없음
unit_collect_until = 3

[advisor]
# "mock"(기본, 네트워크·과금 없음) | "live"(TypeSafe API 키 필요, 과금) | "off"(통계 전용). CLI --no-jev → "off".
# 설정 화면의 체크박스 "Jev 실시간 판단 사용"(켬=live, 끔=mock) / "추천에서 Jev를 빼기"(off)가 여기에 쓴다.
# 오버레이 트레이 메뉴의 "Jev 실시간 판단 (과금)" 체크도 실행 중에 이 값을 바꿔 저장한다.
# 우선순위: CLI(--jev/--no-jev, 이번 실행 고정) > 트레이 토글 > 이 값.
jev_backend = "mock"
jev_enabled = true            # false → fallback_reason = jev_disabled
timeout_s = 2.0               # 추천 1회 전체 예산(초)
max_candidate_comps = 8       # 코드 1차 필터 후 Jev에 보낼 덱 후보 수 (1~20)
cache_size = 64               # state 해시 LRU 캐시 (0 = 끔)
jev_model = "jev-latest"      # "jev-latest" 또는 고정 버전 "jev-1.13.0". 캐시 키·state_hash에 포함
jev_timeout_s = 1.2           # Jev 시도 1회 타임아웃 (jev_timeout_s <= jev_retry_budget_s < timeout_s)
jev_retry_budget_s = 1.5      # RetryPolicy 총 예산
jev_max_retries = 1           # 0~3
circuit_fail_threshold = 3    # 연속 실패 이 횟수면 서킷 오픈
circuit_cooldown_s = 60       # 서킷 오픈 유지 시간

[stats]
primary = "metatft"
fallbacks = ["tactics_tools", "opgg_mcp"]
db_path = "data/stats/stats.sqlite"
days = 3
rank_filter = "CHALLENGER,DIAMOND,GRANDMASTER,MASTER"   # 순서 무관(집합 비교), 로드 시 정렬
request_interval_s = 1.2
keep_snapshots = 5            # 출처별 보존 스냅샷 수 (>= 1)
user_agent = "tft-advisor-research/0.1 (personal use)"

[ui]
# Phase 4에서 오버레이는 PySide6로 확정. "auto" = PySide6가 있으면 오버레이, 없으면 콘솔.
# "console" = 오버레이 없이 콘솔 출력(CLI --no-overlay와 같음). "tkinter"는 미구현(경고 후 콘솔).
backend = "auto"
opacity = 0.85           # [overlay] opacity 가 없을 때의 값
click_through = true     # [overlay] click_through 가 없을 때의 값
max_target_comps = 3     # 오버레이에 표시할 목표 덱 수(advisor 순서 그대로, 점수로 재정렬하지 않음)
# 인식 확인 창: 보드·벤치 유닛(이름·성급·자리·아이템·신뢰도)과 장착/미사용 아이템을 실시간으로 보여 줍니다.
# 우선순위: CLI(--test-view / --no-test-view) > 오버레이 트레이 "인식 확인 창" 체크(여기에 저장) > 이 값.
# --live --no-overlay 에서는 같은 내용을 콘솔에 출력합니다. 설정 화면의 "인식 확인 창 표시" 체크박스도 여기에 씁니다.
test_view = false

[overlay]
enabled = true
anchor = "top_right"     # "top_left" | "top_right" | "bottom_left" | "bottom_right"
x = 24                   # anchor 모서리로부터 가로 여백 px
y = 24                   # 세로 여백 px
width = 460              # 창 너비 px (고정)
height = 960             # 창 높이 px (고정 — 내용이 바뀌어도 늘었다 줄었다 하지 않는다. 화면보다 크면 화면 높이)
# 섹션별 최대 줄 수. 넘치면 "… 외 N", 비어도 자리를 지킨다. 높이가 모자라면 증강 → 아이템 → 상점 → 보드 배치 → 목표 덱 순으로 줄인다
lines_comp = 3           # 목표 덱 1개당 설명 줄(머리 줄·유닛 아이콘 줄 제외)
lines_board = 7
lines_shop = 5
lines_item = 4
lines_augment = 4
unit_icons = true        # 목표 덱 최종 유닛 아이콘 줄(data/templates/18/champions/, 없으면 첫 실행에 받는다 — 못 받으면 이름 글자)
icon_size = 28           # 아이콘 한 변 px(scale 1.0 기준)
scale = 1.0              # 글꼴·여백 배율 (0.5 ~ 3.0). 설정 화면의 "오버레이 글꼴 배율"
# 오버레이 불투명도 0.2 ~ 1.0. 이 키가 없으면 [ui] opacity 를 쓴다. 설정 화면의 "오버레이 불투명도"가 여기에 쓴다.
# opacity = 0.85
# click_through = true   # 생략하면 [ui] click_through
locked = true            # true = 클릭 통과(게임 입력을 가리지 않음). false = 드래그로 이동 가능
always_on_top = true
screen = 0               # 여러 모니터일 때 Qt 화면 번호
remember_position = true # _state/overlay.json 에 저장된 위치를 우선 사용

[logging]
level = "INFO"
recommendation_log = "logs/recommendations.jsonl"
save_frame_on_error = true
