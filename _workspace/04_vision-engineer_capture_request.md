# 게임 화면 캡처 요청서

작성: vision-engineer / 2026-09-22 (QA 04 반영 개정판)

**왜 필요한가요?** 지금 가진 예시 화면 7장은 방송 화면을 캡처한 것입니다. 채팅창과 방송 장식이 섞여 있고 크기도 제각각이라서, 프로그램이 화면의 어느 위치를 읽을지 정확히 맞출 수 없습니다. 게임 화면을 직접 찍은 원본이 있으면 위치를 맞추고, 얼마나 정확하게 읽는지 측정하고, 아이템 그림도 실제 화면 기준으로 새로 만들 수 있습니다.

**하실 일은 세 가지입니다.**
1. 아래 1절대로 설정을 확인합니다.
2. 2절의 화면을 찍어서 폴더 하나에 모읍니다.
3. 4절의 질문에 짧게 답합니다.

정답 파일(JSON)은 직접 쓰지 않으셔도 됩니다. 스크린샷만 주시면 Claude가 초안을 만들고, 틀린 값만 고쳐 주시면 됩니다(3절).

---

## 1. 찍기 전에 확인할 설정

| 항목 | 이렇게 해 주세요 | 알려 주실 값 |
|---|---|---|
| 해상도 | 1920 x 1080 권장 | 실제로 쓰는 해상도 |
| 화면 모드 | **테두리 없는 창모드** (전체화면이면 캡처가 까맣게 나올 수 있어요) | 전체화면 / 테두리 없음 / 창 |
| 인게임 HUD(UI) 크기 | 바꾼 적 없으면 그대로 | "기본" 또는 슬라이더 값 |
| 게임 언어 | 한국어 | |
| Windows 디스플레이 배율 | 100% 권장 | 100 / 125 / 150% |
| **채팅창** | **최소화하거나 숨겨 주세요.** 예전 예시 2장에서 채팅이 레벨 숫자를 가렸어요 | |
| **HDR** | **꺼 주세요** (Windows 설정 → 디스플레이 → HDR). 켜져 있으면 캡처 색이 바랩니다 | |
| **그래픽 필터·색약 모드** | **꺼 주세요** (NVIDIA 필터, 게임 내 색약 모드 등). 아이콘 색이 달라집니다 | |
| 오버레이 | OP.GG, Blitz, Mobalytics, 디스코드 오버레이를 **꺼 주세요** | 끈 것 |
| 모니터 | 게임이 뜨는 모니터가 몇 번인지, 모니터가 몇 개인지 | |

### 찍는 방법

- **1순위: `Win` + `PrtSc`** (Windows 키를 누른 채 PrtSc).
  화면이 한 번 살짝 어두워졌다 돌아오면 저장된 것입니다. 파일은 **`사진` → `스크린샷`** 폴더에 PNG로 자동 저장됩니다.
- 쓰지 말아 주세요.
  - **`PrtSc`만 누르기**: Windows 11에서는 부분 캡처 도구가 열립니다. 화면 일부만 잘리거나 저장이 안 될 수 있어요.
  - `Win` + `Shift` + `S` (부분 캡처)
  - 카카오톡·디스코드로 보낸 파일 (화질이 떨어집니다), 방송 화면 캡처
- 게임 스크린샷 키(`F12`)를 쓰셔도 됩니다. 저장 위치는 보통 `C:\Riot Games\League of Legends\Screenshots`입니다.
  **파일 이름이 `.jpg`로 끝나면 쓰지 말고** `Win` + `PrtSc`로 다시 찍어 주세요(JPG는 화질이 손상됩니다).
- **확인**: 파일을 마우스 오른쪽 클릭 → 속성 → 자세히 → 크기가 **1920 x 1080**(또는 알려 주신 해상도)이면 됩니다.

### 파일을 모으는 곳

- 폴더 하나에 모아서 전달해 주세요. 프로젝트 안에 넣으신다면 `tests\fixtures\screens\raw\` 폴더입니다.
  **이 폴더는 아직 없으니 새로 만들어 주세요.**
- 파일 이름은 `스테이지_화면.png` 형식이면 좋습니다(예: `3-2_준비.png`, `2-1_증강.png`, `3-4_캐러셀.png`). 헷갈리면 그냥 두셔도 됩니다.
- **주의**: 화면 오른쪽 플레이어 목록에 **다른 사람들의 소환사 이름**이 찍힙니다. 이 캡처를 공개 저장소(GitHub 공개 등)에 올리지 말아 주세요.

---

## 2. 찍을 화면 (★ 꼭 필요 / ☆ 있으면 좋음)

| # | 화면 | 장수 | 꼭 보여야 하는 것 | 어디에 쓰나요 |
|---|---|---|---|---|
| 1 ★ | **준비 단계, 상점 5칸이 모두 찬 상태.** 스테이지 2, 3, 4에서 각각 | 3~5 | 아래 상점 5칸, 왼쪽 아래 "N레벨 a/b", 골드, 연승 숫자, 상점 확률 줄, 맨 위 스테이지 | 가장 중요. 상점과 숫자 읽기 |
| 2 ★ | 준비 단계 **레벨 7 이상** (7, 8, 가능하면 9) | 2 | "N레벨 a/b", 상점 확률 | 7레벨 이상 경험치 표 확인 |
| 3 ★ | 상점에서 1~2칸을 **사서 빈칸이 있는** 상태 | 1 | 빈칸 | 빈칸 알아보기 |
| 4 ★ | **왼쪽 세로 아이템 칸(10칸)** 에 아이템 3개 이상. 가능하면 완성 아이템, 상징, 자석 제거기도 | 2~3 | 왼쪽 세로 칸 전부 | 아이템 그림을 실제 화면으로 새로 만들기 |
| 5 ★ | **증강 선택 화면** 2-1, 3-2, 4-2 (실버·골드·프리즘이 섞이면 좋음) | 3 | 카드 3장 이름, "하나 선택" 제목 | 증강 이름 읽기, 등급(I/II/III) 구별 |
| 6 ★ | 증강을 **고른 직후**의 준비 단계 | 1~2 | 고른 증강이 화면 어딘가에 표시되는 곳 | 보유 증강 위치 찾기 |
| 7 ★ | **전투 중** 화면 (같은 라운드의 준비/전투 한 쌍이면 가장 좋음) | 2 | 맨 위 스테이지 막대, 타이머 | 전투와 준비를 구별하기(지금은 못 합니다) |
| 8 ★ | **연패 중** 준비 단계 (연패 2 이상) | 1 | 골드 오른쪽 불꽃/물방울 아이콘과 숫자 | 연승/연패 구별 |
| 9 ★ | **캐러셀**(가운데 원형으로 유닛이 도는 공동 선택) 2-4 또는 3-4 | 1 | 맨 위 스테이지 | 캐러셀 알아보기 |
| 10 ☆ | 대기석 9칸이 다양하게 찬 상태 + 보드 유닛 5명 이상 | 1~2 | 대기석 전체 | 다음 단계(보드 인식) |
| 11 ☆ | 왼쪽 **특성 패널**에 특성이 8개 이상 | 1 | 특성 패널 전체 | 특성 패널이 넘칠 때 모양 |
| 12 ☆ | 로딩 화면, 게임 종료(등수) 화면 | 각 1 | | 게임 밖/종료 알아보기 |
| 13 ☆ | 모루·포탈 등 특수 선택 화면(있다면) | 1~2 | 선택 카드 | 아이템 선택 화면 알아보기 |
| 14 ☆ | 1920x1080이 아닌 해상도를 쓴다면, 그 해상도로 1번 화면 한 장 더 | 1 | | 다른 해상도 지원 |

★만 모으면 약 16~20장입니다.

---

## 3. 정답 확인 (Claude가 초안 작성 → 틀린 것만 고치기)

스크린샷을 주시면 Claude가 화면마다 **정답 초안**을 만들어 드립니다. 이런 모양의 표입니다.

| 항목 | 초안 값 | 맞나요? |
|---|---|---|
| 스테이지 | 3-2 | |
| 레벨 / 경험치 | 6 / 4/36 | |
| 골드 | 42 | |
| 연승·연패 | 연패 2 | |
| 상점 (왼쪽부터) | 카르마, 니달리, (빈칸), 라칸, 3단계와 함께(9골드) | |
| 왼쪽 아이템 칸 (위에서부터) | 자석 제거기, B.F. 대검, 연습용 장갑, 고속 연사포 | |
| 증강 선택지 | 고위천사의 지팡이, 출정, 수완가 | |

- **틀린 칸만 고쳐 주세요.** 이름은 게임에 보이는 **한국어 그대로** 쓰시면 됩니다. 코드가 알아서 내부 ID로 바꿉니다.
- 가려져서 안 보이는 값은 "안 보임"이라고 적어 주세요. 그 항목은 채점에서 뺍니다.
- 짐작한 값이면 "짐작"이라고 적어 주세요.

직접 파일로 쓰고 싶으시면 부록 A의 형식을 쓰시면 됩니다(선택).

---

## 4. 질문 (짧게 답해 주세요)

> **2026-09-22 갱신**: 16:10 캡처 6장을 받아서 일부는 답이 나왔습니다. 아래 표에서 **"남은 질문"** 칸만 답해 주시면 됩니다.
> 자세한 내용은 `_workspace/05_vision_draft_labels.md`(사용자용)와 `_workspace/05_vision_aspect_and_labels.md`(개발 보고)에 있습니다.

| # | 질문 | 알아낸 것 | 남은 질문 |
|---|---|---|---|
| 1 | **연패 아이콘** | 골드 오른쪽 아이콘은 **불꽃 하나**이고 색만 바뀝니다. **연승 1 이상 = 주황 불꽃**, **연승 0 = 회색 불꽃**(6장에서 확인). 예전에 "파란 물방울"로 적은 것은 잘못 본 것입니다 | **연패일 때 색이 무엇인가요?** 연패 2 이상 준비 화면 **1장**만 주시면 끝납니다. 그때까지 연승·연패 숫자는 추천에 쓰지 않습니다 |
| 2 | **"2-4 아이템선택" = 캐러셀인가?** | 이번 6장에 캐러셀이 없어 확인하지 못했습니다. 예전 화면은 여전히 캐러셀(공동 선택)로 보입니다 | **파일 이름의 "아이템선택"이 캐러셀을 뜻한 게 맞나요?** 따로 아이템만 고르는 화면이 있다면 그 화면 1장 |
| 3 | **연승·연패를 추천에 쓸지** | 숫자 읽기는 6장에서 4/4 정확했습니다. 막고 있는 것은 **부호(연승인지 연패인지)** 뿐입니다 | 1번이 풀리면 바로 켭니다. 지금 방식(연승·연패 0일 때만 사용)으로 두어도 괜찮나요? |
| 4 | **첫 아이템 칸의 노란 숫자** | 아이콘이 Riot 원본의 "아이템 제거기 — 남은 횟수 N" 그림과 **정확히 일치**합니다. 그런데 같은 판에서 숫자가 **2 → 4 → 5 → 6 → 6 으로 늘었습니다**(1-3 → 6-3) | **판이 진행되면서 사용 횟수가 늘어나는 게 맞나요?** 아니면 개수를 세는 건가요? (인식 결과에는 영향 없습니다) |
| 5 | **인게임 HUD 크기** | 확인 못 했습니다 | 기본값에서 바꿔 쓰시나요? |
| 6 | **화면 해상도·모드** (신규) | 받은 파일이 1273~1279 x 795~802 로 장마다 조금씩 다릅니다(16:10). **원본 1280x800에서 가장자리가 몇 px 잘린 것**으로 보입니다 | **실제 게임 해상도가 얼마인가요? 창모드인가요 전체화면인가요?** 앱에 화면 크기 옵션을 넣었으니(`[vision] resolution`, `aspect`) 값을 알려 주시면 고정해 드립니다 |
| 7 | **HUD 오른쪽 끝 "6" 상자** (신규) | 하단 HUD 오른쪽 끝(상점 5번째 칸 위)에 **초록 잎 장식 동그라미 + 숫자 "6"** 이 있습니다. 6장 모두 "6"입니다 | **이게 무엇을 세는 값인가요?** (지금은 읽지 않습니다) |
| 8 | **증강 등급(실버/골드/프리즘) 구별** (신규) | 카드 **테두리**는 세트 테마(초록 잎)라 등급이 드러나지 않습니다. 가운데 **아이콘 색**이 신호로 보입니다(2-1 화면 3장 모두 금색 = 골드) | 실버·프리즘이 섞인 증강 화면 1~2장 주시면 색 ↔ 등급 대응을 확정합니다 |

### 아직 필요한 화면 (2026-09-22 기준, 우선순위 순)

| 순위 | 화면 | 왜 필요한가요 |
|---|---|---|
| 1 ★ | **같은 라운드의 준비 + 전투 한 쌍** | 준비와 전투를 구별하지 못합니다(전투를 준비로 봅니다). 이번 6장에 전투가 3장 있었지만 쌍이 없어 규칙을 확정하지 못했습니다 |
| 2 ★ | **연패 2 이상 준비 화면** 1장 | 질문 1 |
| 3 ★ | **증강을 고른 직후** 준비 화면 1~2장 | 내가 가진 증강을 화면에서 읽기(덱 선정의 핵심 입력) |
| 4 ☆ | 캐러셀(2-4 또는 3-4) 1장 | 질문 2 |
| 5 ☆ | 게임 종료(등수) 화면, 로딩 화면 각 1장 | 게임 밖/종료 알아보기 |
| 6 ☆ | 아이템 선택·모루 선택 화면(있다면) 1~2장 | 질문 2 |
| 7 ☆ | 실버·프리즘이 섞인 증강 선택 화면 | 질문 8 |

> **이미 채워진 것**(다시 안 찍으셔도 됩니다): 준비 단계 상점 5칸(2·4·7·9레벨), 7레벨 이상 경험치, 왼쪽 아이템 칸(최대 5개, 유물 포함), 증강 선택 화면, 특성 패널 10줄 이상, 보드 유닛 12기 이상, 16:10 해상도.

---

## 부록 A. 정답 파일 형식 (직접 쓰실 때만)

스크린샷과 같은 이름으로 `.expected.json`을 만듭니다(예: `3-2_준비.png` → `3-2_준비.expected.json`).

```json
{
  "_note": "원본 1920x1080, 테두리 없음, HUD 기본, 한국어",
  "screen_mode": "planning",
  "stage": "3-2",
  "level": 6,
  "xp": [4, 36],
  "gold": 42,
  "streak": -2,
  "hp": 71,
  "shop_odds": [30, 40, 25, 5, 0],
  "shop": [
    {"name": "카르마"},
    {"name": "니달리"},
    null,
    {"name": "라칸"},
    {"special": "3단계와 함께", "cost": 9}
  ],
  "item_bench": ["자석 제거기", "B.F. 대검", "연습용 장갑", "고속 연사포", null, null, null, null, null, null],
  "augment_offer": ["고위천사의 지팡이", "출정", "수완가"]
}
```

- 모르거나 안 보이는 항목은 **줄을 통째로 빼 주세요.**
- `screen_mode`: `planning`(준비) / `combat`(전투) / `augment_select`(증강 선택) / `carousel`(캐러셀) / `item_select` / `loading` / `game_over`
- `streak`: 연승 `+N`, 연패 `-N`, 없으면 `0`
- `shop`: 왼쪽부터 5칸. 빈칸은 `null`. 특수 상품은 `{"special": 이름, "cost": 가격}`
- `item_bench`: 왼쪽 세로 아이템 칸을 **위에서부터 10칸** 순서대로, 게임에 보이는 한국어 이름으로. 빈칸은 `null`.
- 짐작한 값에는 `"note_필드이름": "이유"`를 함께 적어 주세요(예: `"note_level": "채팅에 가려 짐작"`).

---

## 부록 B. 개발자 메모 (사용자는 읽지 않아도 됩니다)

- **item_bench 라벨 = 한국어 표시 이름**으로 통일했다. `harvest-items`가 정적 데이터로 apiName(묶음 대표 ID)으로 바꾸고, 못 바꾸는 이름은 저장하지 않고 오류로 보고한다(QA04-V1 수정). 라벨에 apiName을 써도 받는다. `note_item_bench` 키도 임시로 읽는다.
- `fixtures.py`(app-integrator, Phase 3 config round에서 반영): `items` 이름 → 대표 ID ItemRef, `item_bench` → `ExpectedScreen.extras["item_bench"]`(대표 ID|None). `items`가 없으면 item_bench로 채워 정확도 비교 대상이 된다. 대표 ID(`ItemCatalog.resolve`)를 쓰므로 자석 제거기도 vision 출력(`DA_Consumable_ItemRemover`)과 ID로 바로 같다.
- 평가 경로: `evaluate.py`와 `test_vision.py` fixture 테스트는 `tests/fixtures/screens/*.expected.json`만 본다(`raw/` 하위는 보지 않음). 원본 캡처는 `python -m tft_advisor.vision.evaluate tests/fixtures/screens/raw`로 따로 평가하고, 라벨이 확정되면 방송 크롭 대신 `screens/`로 옮길지 결정한다.
- 원본 캡처와 `items_screen/` 템플릿은 Riot 아트워크 + 타 플레이어 이름이 있으므로 공개 저장소에 올리지 않는다(`data/templates/README.md`).
- 캡처 순서 제안: #1 → #4(harvest) → #5 → #8 → #7 → 나머지.

### 2026-09-22 갱신 메모 (16:10 캡처 6장 반영)

- 받은 캡처는 **16:10 전체 UI**였다(1273~1279 x 795~802). 16:9 ROI는 가로가 빗나갔고, `regions.py`에 비율 규칙(CENTER/LEFT/RIGHT anchor)과 실측 `SET18_16X10`을 넣어 해결했다. `_workspace/05_vision_aspect_and_labels.md` 참고.
- **`[vision]` 화면 크기 설정 추가**: `resolution`(기본 auto), `aspect`(기본 auto), `profile`(기본 auto로 변경, 옛 "1920x1080"도 동작), `content_box_auto`(기본 true). 사용자가 해상도를 알려 주면 못박을 수 있다.
- **1절의 "해상도 1920x1080 권장"은 더 이상 필수가 아니다.** 쓰시는 해상도 그대로 찍으셔도 된다(16:9·16:10 모두 실측 지원). 다만 **한 번 정하면 바꾸지 마시고**, 값을 알려 주시면 설정에 고정한다.
- 3절의 "정답 초안" 흐름은 그대로 동작했다. 초안은 `tests/fixtures/screens/raw/*.expected.json`과 `_workspace/05_vision_draft_labels.md`에 있다.
- `harvest-items`가 저장하는 실화면 템플릿의 기하 버그를 고쳤다(저장본이 자기 자신과도 안 맞았다). 6장으로 아이템 템플릿 10개를 수집했고 items 신뢰도가 0.83~0.87 → 0.94~0.97이 되었다.
- 부록 B의 "평가 경로" 메모는 그대로 유효하다: `python -m tft_advisor.vision.evaluate tests/fixtures/screens/raw`.
